"""Admin-side routers for the Connector dashboard.

ADR-025 Phase 4 introduces the audit router (read-only audit-log
viewer, gated by the admin secret). Phases 1, 2 and 5 will add the
users router and the auth router alongside it.
"""
